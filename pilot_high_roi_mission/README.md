# Pilot High Roi Mission

This folder contains the **pilot_high_roi_mission.py** script, which is part of Ego's evolved capabilities workflow.

## Overview

This script defines utility routines for the project.

## Dependencies

- `datetime`
- `json`
- `os`

## Usage

You can import and use the components of this script in Python:
```python
import pilot_high_roi_mission.pilot_high_roi_mission
```

Alternatively, run it directly from the parent directory:
```bash
python -m pilot_high_roi_mission.pilot_high_roi_mission
```