# Animate Smoke

This folder contains the **animate_smoke.py** script, which is part of Ego's evolved capabilities workflow.

## Overview

This script defines the following function: `create_smoke_frame`.

## Functions

### `def create_smoke_frame(base_img, frame_num, total_frames)`

No description provided.

## Dependencies

- `PIL`
- `numpy`
- `random`

## Usage

You can import and use the components of this script in Python:
```python
from animate_smoke.animate_smoke import create_smoke_frame
```

Alternatively, run it directly from the parent directory:
```bash
python -m animate_smoke.animate_smoke
```