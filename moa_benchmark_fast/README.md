# Moa Benchmark Fast

This folder contains the **moa_benchmark_fast.py** script, which is part of Ego's evolved capabilities workflow.

## Overview

This script defines the following functions: `standard_attention`, `moa_vectorized_row_attention`, `moa_blocked_attention`.

## Functions

### `def standard_attention(Q, K, V)`

No description provided.

### `def moa_vectorized_row_attention(Q, K, V)`

No description provided.

### `def moa_blocked_attention(Q, K, V, block_size)`

No description provided.

## Dependencies

- `numpy`
- `time`

## Usage

You can import and use the components of this script in Python:
```python
from moa_benchmark_fast.moa_benchmark_fast import standard_attention
```

Alternatively, run it directly from the parent directory:
```bash
python -m moa_benchmark_fast.moa_benchmark_fast
```