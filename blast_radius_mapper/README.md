# Blast Radius Mapper

This folder contains the **blast_radius_mapper.py** script, which is part of Ego's evolved capabilities workflow.

## Overview

This script defines the following function: `map_blast_radius`.

## Functions

### `def map_blast_radius(alerts)`

No description provided.

## Dependencies

- `json`
- `re`

## Usage

You can import and use the components of this script in Python:
```python
from blast_radius_mapper.blast_radius_mapper import map_blast_radius
```

Alternatively, run it directly from the parent directory:
```bash
python -m blast_radius_mapper.blast_radius_mapper
```